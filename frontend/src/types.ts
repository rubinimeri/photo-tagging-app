export type Coordinates = {
  x: number | null;
  y: number | null;
};

export type Game = {
  id: number;
  name: string;
  url: string;
};

export type Character = {
  url: string;
  name: string;
};
